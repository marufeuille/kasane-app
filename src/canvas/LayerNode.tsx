/**
 * LayerNode（skeleton）— 単一レイヤーの描画ノード。
 * react-konva による移動 / 拡縮 / 回転 / 透過(不透明度) は
 * S3.1〜S3.3（dt-5ae 配下）で実装する。Konva 統合前の参照点として残す。
 */
export default function LayerNode() {
  // 実装は S3.1（dt-5ae）。本 skeleton 時点では UI 上のスタブ。
  return null
}
