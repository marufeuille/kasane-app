/**
 * Gemini（`gemini-2.5-flash-image`, nano banana）クライアントのエントリポイント（skeleton）。
 *
 * `:generateContent` の薄ラッパ・インライン base64 PNG 取得・aspectRatio / 解像度指定は
 * S2.1（dt-kd9 配下）で実装する。CORS は dev proxy / Cloudflare Worker でフォールバック（S2.2）。
 */

/** このモジュールは S2.1（dt-kd9 / E2）で実装される。 */
export const GEMINI_CLIENT_TODO = 'Implemented in dt-kd9 (S2.1)'
