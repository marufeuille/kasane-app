/**
 * SettingsPanel（skeleton）— BYOK（Bring Your Own Key）設定。
 * Gemini API キーを IndexedDB にローカル保存（平文保存の警告表示）する UI は
 * S1.3（dt-4n4.3）で実装する。ここではプレースホルダー。
 */
export default function SettingsPanel() {
  return (
    <section className="panel">
      <h2 className="panel__title">Settings</h2>
      <p className="panel__hint">Gemini API キーをローカル保存（BYOK）— S1.3</p>
    </section>
  )
}
